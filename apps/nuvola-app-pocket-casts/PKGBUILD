# Maintainer: Laramy Black <laramy2020@gmail.com>

pkgname=nuvola-app-pocket-casts
pkgver=1.3
pkgrel=1
pkgdesc="pocket casts integration for Nuvola Player."
arch=("any")
url="https://github.com/tiliado/nuvola-app-pocket-casts"
license=('custom:BSD')
depends=('nuvolaruntime' 'imagemagick')
makedepends=('nuvolasdk' 'scour')
source=(https://github.com/tiliado/${pkgname}/archive/${pkgver}.tar.gz)
sha256sums=('79fa0a546f20e846ab91bba22cdf87e2e0b0685fae4156eea13f607de65b002a')

build() {
    cd "$srcdir/${pkgname}-${pkgver}"
    ./configure --prefix=/usr 
    make all
}

package() {
    cd "$srcdir/${pkgname}-${pkgver}"
    make DESTDIR="${pkgdir}" install
}
